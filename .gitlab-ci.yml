# This file is a template, and might need editing before it works on your project.
# You can copy and paste this template into a new `.gitlab-ci.yml` file.
# You should not add this template to an existing `.gitlab-ci.yml` file by using the `include:` keyword.
#
# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/C++.gitlab-ci.yml

# use the official gcc image, based on debian
# can use versions as well, like gcc:5.2
# see https://hub.docker.com/_/gcc/

image: gcc

build:
  stage: build
  # instead of calling g++ directly you can also use some build toolkit like make
  # install the necessary build tools when needed
  before_script:
    - apt update && apt -y install make autoconf cmake 
  # 1) Remove existing build files
  #- rm -rf builds_debug
  #- mkdir builds_debug
  # 2) Get latest repository changes and move into builds_dir
  #- cd open-csmp
  #- git fetch
  #- git switch development 
  #- git pull origin 
  #- cd ../builds_debug

  script:
      # 1) Get latest repository changes
     - git fetch
     - git switch development 
     - git pull origin 
     # 2) Clone third party software (eigen)
     - cd thirdparty/eigen/
     - rm -rf * 
     - git clone https://gitlab.com/libeigen/eigen.git  ##RECLONES EIGEN EVERY TIME (NOT NEEDED)
     - cd ../../
      # 3) Remove existing build files and move into it 
     - rm -rf ../builds_debug
     - mkdir ../builds_debug
     - cd ../builds_debug
      # 4) Run CMake and Make: generate build files 
     - cmake -S ../open-csmp/ -B .
     - export NPROC=$(echo | nproc)
     - cmake --build . --target all -j ${NPROC} #TODO Change 4 to nproc

# run tests using the binary built before
test:
  stage: test
  script:
     - pwd
     - cd ../builds_debug
     - cp -r ../open-csmp/tests/dataset . 
     - cd dataset
     - pwd 
     - ls ../tests/*
     - ../tests/csmptest_development #CHECK THIS WORKS
     # - .tests/csmptests_main. #Replace with SKM test 
    #- ./runmytests.sh
