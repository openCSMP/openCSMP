EclipseInterface - key operations
=================================
SKM log 4/4/2018

Notes 
- regions file needs to be supplied separately, non-listed regions wont go to CSMP

- EclipseModelSettings
  --------------------
ASCII file interface to read corner point grid from ( *.GRDECL, *.grdecl ) file.
In order to build a native CSMP Model the provided grid information is assigned to 
VSet and ModelTopology objects.
The regions that are constructed by default are:
% regions which include all the cells
MATRIX
MATRIX_ACTIVE
MATRIX_INACTIVE
% and specific fault regions

In addition to grid information the properties such as:
'permeability', 'porosity', 'pressure', 'saturation' can be readed as well.
User is able to control property type and placement.
For example one can create tensor or scalar permeability variables or
to place 'pressure' at the nodes or elements.



Program Flow - reading a .GRDECL file
-------------------------------------
- construct EclipseModel
	- EclipseModelSettings::EclipseModelSettings
		- readDesiredRegions if there, else only construct MATRIX (if not empty)
	- MeshSetUp
	- PoroPropertySetup
	- PermPropertySetup
	- RockNumPropertySetup

- EclipseModel::BuildModel  - EclipseModel::EclipseModelSettings: regions_:std::set<std::string>
	- EclipseInterface::ReadFile   - grid_:CornerPointGrid
		- EclipseInterface::ReadFile
			- reader:TextFileInterface<EclipseInterface<dim> > - reads block data into EclipseInterface
				- EclipseInterface::ReadDimensions
				- ...
				- EclipseInterface::Read_COORD
				- EclipseInterface::Read_ZCORN
				- EclipseInterface::ReadFaultsData
				- EclipseInterface::ReadActiveCells
				- EclipseInterface::ReadScalarProperty (porosity)
				- EclipseInterface::ReadTensorProperty (permeability)
				- ...
				- EclipseInterface::ReadWellSpecs
				- ...
 		- CornerPointGrid::CreateModel
			- Initialise grid specs and map between pillar and cell nodes
			- Initialize polyhedral cells (including cells with wells)
			- Assign cell data to VSet
				- processing non degenerate cells only meaning that there will be no tetrahedra
				- processing degenerate cells
			- Assign node coordinates
			- Establishing Model Topology
				- CornerPointGrid::EstablishActiveDomain
				- CornerPointGrid::EstablishFaultRegions
				- CornerPointGrid::EstablishWellRegions
			- Reporting
				- CornerPointGrid::CreateModel: VSet resulting from
		 - EclipseInterface::WritePropertiesToVSet
  			
	     branch-point in model building: continue with 1) or without 2) region data
			
	- assigning regions, faults, and wells
	- checking that properties exist in CSMP model
	- Model<dim>::Initialize:  with or without ModelTopology data		 	
			
